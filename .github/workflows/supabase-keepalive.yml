# Maintient le projet Supabase actif : l'offre gratuite met un projet en pause
# après 7 jours d'activité jugée trop faible. La doc Supabase indique que
# « quelques requêtes par jour » suffisent typiquement : un ping quotidien
# se place dans cette zone et rend négligeable le risque qu'une exécution
# planifiée saute (les crons GitHub peuvent être retardés ou ignorés en
# période de charge).
#
# ⚠️ GitHub désactive les workflows planifiés après 60 jours sans activité
# (commit) sur le dépôt : un e-mail « scheduled workflow disabled » est envoyé,
# et il suffit de cliquer « Enable workflow » dans l'onglet Actions.
name: Supabase keepalive

on:
  schedule:
    # Tous les jours à 06:47 UTC (minute décalée : les crons à l'heure pile
    # sont les plus retardés par GitHub).
    - cron: '47 6 * * *'
  workflow_dispatch: # déclenchement manuel possible depuis l'onglet Actions

permissions: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping de l'API REST Supabase
        # Requête anodine : SELECT id sur notifications avec la clé anon.
        # La RLS réserve la lecture aux utilisateurs authentifiés : la réponse
        # est un tableau vide (200), aucune donnée n'est exposée — mais la
        # requête compte comme de l'activité API et empêche la mise en pause.
        # ⚠️ Ne pas cibler une table dont la policy RLS appelle une fonction du
        # schéma public (logements, reservations…) : la migration 076 retire à
        # anon le droit EXECUTE sur ces fonctions, et la requête échoue en 401.
        # La policy de notifications n'utilise que auth.uid()/auth.role().
        run: |
          code=$(curl --silent --show-error --output /tmp/body --write-out '%{http_code}' \
            "${SUPABASE_URL}/rest/v1/notifications?select=id&limit=1" \
            -H "apikey: ${SUPABASE_ANON_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_ANON_KEY}")
          echo "Réponse HTTP : ${code}"
          if [ "${code}" != "200" ]; then
            echo "::error::Ping Supabase en échec (HTTP ${code}) — le projet est peut-être en pause."
            cat /tmp/body
            exit 1
          fi
        env:
          SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
